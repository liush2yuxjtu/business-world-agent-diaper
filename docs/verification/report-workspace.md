# 报告保存与独立备注验收

报告中心接入新建、数据库保存、历史选择、固定快照、人工备注、版本冲突拒绝与刷新恢复。当前沿用免登录演示：随机浏览器标识使用 HttpOnly / SameSite Cookie，数据库仅存其哈希，RLS 按请求头中的哈希隔离报告。没有修改既有业务表权限。清除浏览器数据或更换浏览器后无法自动找回报告，页面明确提示下载留存。

迁移：`lib/business-world/supabase-report.sql`。2026-09-21 已在既有 Supabase 项目的官方编辑器执行成功；实际请求确认匿名读取不泄露记录。部署前须在目标数据库应用该迁移。

验证范围：
- 10 项业务边界与报告模型测试、类型检查、Next.js 生产构建通过。
- 真实 HTTP 新建报告 `1c14bb23-0069-40b2-824f-e60b24990d76`，保存并回读，人工备注更新为 revision 2；摘要及完整来源快照不变。
- 无浏览器标识读取精确报告为 404、列表为空；直接匿名 Supabase 查询也为空。
- 相同报告旧版本写入返回 409；跨站写入 403，无效 ID / 空标题 400。
- 前后完整经营基线相等，SHA256 `5dd03c43d0b71dfb6f0dae4e6f61eac699ebaf10682df7bbc89bbca04ad27d24`。
- 本地真实浏览器新建 `4a6f2986-2f06-4b7a-80f0-fb4e78f0e158`，标题“浏览器报告验收：夜间产品复盘”，人工备注保存并刷新恢复成功；编辑中禁用报告切换和下载，保留未保存提示。

当前下载仍为所选已保存报告的文本，包括独立人工备注。PDF / PPT、只读分享、邮件草稿与 Agent 同源报告操作尚待完成，不能据此认定整个 canonical 报告中心或完整 HTML/MD 验收通过。未启用真实投放、交易或邮件发送。
