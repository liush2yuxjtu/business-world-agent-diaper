import {METRICS,userText as e,formatMetric as fmt} from './ui-state.mjs';

// Layout and copy only. Persistence, authorization and calculations stay in the existing runtime.
export const SCREENS=Object.freeze({
 overview:{n:'01',label:'经营总览',en:'BUSINESS OVERVIEW',title:'把经营记录，变成下一步行动。',text:'从消费者洞察到内容、直播与增长，在同一个工作区看清依据、比较选择、整理行动。',next:'world',nextText:'查看商业关系',symbol:'◎',stage:0,keys:['gmv','conversion','roi','repeat']},
 persona:{n:'02',label:'消费者洞察',en:'PERSONA STUDIO',title:'先理解人，再决定如何沟通。',text:'把待验证的人群假设整理成研究任务，再带着人群背景进入内容策略。',next:'content',nextText:'衔接内容策略',symbol:'♙',stage:1,keys:[]},
 world:{n:'03',label:'业务数据',en:'WORLD BUILDER',title:'看见指标之间的关系。',text:'从一条经营记录出发，查看来源、探索关联因素，再比较不同假设。关联不等于因果。',next:'experiment',nextText:'比较一个经营假设',symbol:'◇',stage:0,keys:[]},
 content:{n:'04',label:'内容策略',en:'CONTENT STRATEGY',title:'把用户需求，写成值得看的内容。',text:'明确人群、核心信息与经营依据。先形成内容提纲，再整理发布计划。',next:'live',nextText:'衔接直播运营',symbol:'▤',stage:2,keys:['engagement']},
 live:{n:'05',label:'直播运营',en:'LIVE OPERATIONS',title:'让每一场直播，都有准备和复盘。',text:'从观看记录与商品线索出发，整理开播准备、讲解要点和复盘任务。',next:'product',nextText:'查看关联商品',symbol:'▣',stage:2,keys:['live','conversion']},
 growth:{n:'06',label:'投放优化',en:'GROWTH OPTIMIZATION',title:'先比较假设，再讨论预算。',text:'查看广告投入产出记录，将效率变化带入情景实验。这里不会调整真实投放。',next:'experiment',nextText:'比较广告效率假设',symbol:'↗',stage:3,keys:['roi','gmv']},
 product:{n:'07',label:'商品分析',en:'PRODUCT ANALYSIS',title:'让好商品，找到更合适的机会。',text:'结合成交、转化与复购记录提出商品问题。SKU 销量、库存和利润需要独立证据。',next:'growth',nextText:'衔接投放策略',symbol:'▢',stage:1,keys:['gmv','conversion','repeat']},
 experiment:{n:'08',label:'情景实验',en:'SCENARIO EXPERIMENTS',title:'在行动之前，多比较一种可能。',text:'明确基准、杠杆和变化幅度。估算结果与原始记录分开保存，不作为销量预测。',next:'report',nextText:'整理为经营报告',symbol:'⚗',stage:3,keys:[]},
 report:{n:'09',label:'经营报告',en:'BUSINESS REPORTS',title:'把依据和选择，交代清楚。',text:'整理固定的经营记录与情景假设，补充人工意见，再明确选择导出或分享。',next:'overview',nextText:'回到经营总览',symbol:'▧',stage:4,keys:[]}
});
const steps=[['world','整理数据'],['persona','理解人群'],['content','准备行动'],['experiment','比较假设'],['report','汇总复盘']];
const btn=(label,attrs,primary=false)=>`<button class="btn ${primary?'primary':''}" ${attrs}>${e(label)} <span aria-hidden="true">↗</span></button>`;
function primary(id){const s=SCREENS[id];return id==='overview'?btn('添加 / 更新经营记录','data-open-source',true):id==='experiment'?btn(s.nextText,'data-scenario-report',true):id==='growth'?btn(s.nextText,'data-simulate="ads"',true):btn(s.nextText,`data-handoff="${s.next}" data-origin="${id}"`,true);}
export function screenHero(id){const s=SCREENS[id];return `<header class="screen-hero" data-screen-hero="${id}"><div class="screen-copy"><div class="screen-eyebrow"><span>${s.n}</span>${s.en}</div><h1 tabindex="-1">${e(s.title)}</h1><p>${e(s.text)}</p><div class="hero-actions">${primary(id)}${btn(id==='overview'?'查看商业关系':'查看经营来源',id==='overview'?'data-handoff="world" data-origin="overview"':'data-open-source')}</div></div><div class="screen-art" aria-hidden="true"><div class="art-orbit orbit-one"></div><div class="art-orbit orbit-two"></div><span class="art-node node-one">来源</span><span class="art-node node-two">行动</span><div class="art-core">${s.symbol}</div><span class="art-tag">${e(s.label)}</span></div></header>`;}
export function journey(id){return `<nav class="journey-nav" aria-label="经营决策流程">${steps.map(([route,label],i)=>`<button class="journey-step ${i===SCREENS[id].stage?'current':''}" data-handoff="${route}" data-origin="${id}" ${i===SCREENS[id].stage?'aria-current="step"':''}><span>${String(i+1).padStart(2,'0')}</span>${label}<i aria-hidden="true">${i===4?'✓':'→'}</i></button>`).join('')}</nav>`;}
const ACTIONS={
 overview:[['准备内容提纲','从目标人群与沟通主题开始。','data-work-draft="brief" data-origin="content"','▤'],['准备直播任务','先记录目标与需要核对的商品。','data-work-draft="task" data-origin="live"','▣'],['比较投放假设','固定基准，再比较效率变化。','data-simulate="ads"','↗']],
 persona:[['整理人群研究问题','记录行为假设、待收集证据和验证方法。','data-work-draft="task" data-origin="persona"','♙'],['查看人群案例','浏览明确标记的案例，不代替真实用户证据。','data-open-examples="persona"','◎'],['进入内容策略','带着人群研究背景继续。','data-handoff="content" data-origin="persona"','▤']],
 content:[['创建内容提纲','填写目标人群、核心信息和经营依据。','data-work-draft="brief" data-origin="content"','▤'],['整理发布计划','先确认渠道与时间；保存不等于发布。','data-work-draft="plan" data-origin="content"','▦'],['回看人群问题','检查内容是否回应了明确的人群需求。','data-handoff="persona" data-origin="content"','♙']],
 live:[['准备开播清单','整理商品、讲解要点与待确认事项。','data-work-draft="task" data-origin="live"','▣'],['查看商品背景','把直播目标与商品问题放在一起。','data-handoff="product" data-origin="live"','▢'],['创建复盘任务','明确记录、证据和下一次改进。','data-work-draft="task" data-origin="live" data-purpose="复盘"','▧']],
 growth:[['比较广告效率','打开预填的效率情景，不直接改预算。','data-simulate="ads"','↗'],['整理投放建议','把目标、依据和风险保存为待确认草稿。','data-work-draft="plan" data-origin="growth"','◈'],['查看内容策略','回看广告承诺与实际内容是否一致。','data-handoff="content" data-origin="growth"','▤']],
 product:[['整理商品问题','先核对 SKU 来源，再讨论库存或利润。','data-work-draft="task" data-origin="product"','▢'],['比较转化假设','用已有基准比较转化率变化。','data-simulate="conversion"','↗'],['研究关联人群','带着商品问题，回到消费者洞察。','data-handoff="persona" data-origin="product"','♙']]
};
function actionCards(id){return `<section class="action-grid" aria-label="${SCREENS[id].label}下一步">${(ACTIONS[id]||[]).map(([title,text,attrs,icon])=>`<button class="action-card" ${attrs}><span class="action-icon" aria-hidden="true">${icon}</span><b>${e(title)}</b><p>${e(text)}</p><span class="action-arrow" aria-hidden="true">↗</span></button>`).join('')}</section>`;}
function metricCards(keys,snapshot,unknown){return `<div class="workbench-metrics">${keys.map(key=>`<button class="workbench-metric" data-metric="${key}" aria-label="查看${METRICS[key].label}详情"><span class="metric-icon" aria-hidden="true">${key==='gmv'?'¥':key==='roi'?'↗':'◫'}</span><span class="metric-name">${e(METRICS[key].label)}</span><strong>${unknown?'—':e(fmt(key,snapshot?.metrics[key]))}</strong><small>${unknown?'暂时无法读取':snapshot?'人工录入 · 尚未核验':'尚无记录'}</small><span class="metric-arrow" aria-hidden="true">↗</span></button>`).join('')}</div>`;}
function sourceCard(snapshot,unknown,place){return `<section class="work-source-card"><div class="section-heading"><h3>数据准备情况</h3><span class="status-pill">${unknown?'待恢复':snapshot?'人工记录':'尚待添加'}</span></div><div class="source-row"><span class="source-icon" aria-hidden="true">▱</span><div><b>${unknown?'暂时无法读取工作区':snapshot?e(snapshot.name):'添加第一条经营记录'}</b><p>${unknown?'请恢复连接后刷新。未读取的记录不是空记录。':snapshot?'记录保存在'+e(place)+'，人工录入尚未核验。':'记录指标、统计周期与来源说明，让后续行动有依据。'}</p></div></div>${btn(snapshot?'查看 / 更新来源':'添加经营记录','data-open-source')}<p class="boundary-copy">平台自动同步尚未接入。</p></section>`;}
export function renderRecordSurface(id,state,{unknown=false,place='此设备'}={}){
 const s=SCREENS[id];const message=unknown?'暂时无法读取工作区记录，请恢复连接后刷新。':id==='persona'?'尚无人群证据。总量记录不能推导出人数、行为分布或置信度。':state.snapshot?`当前来源：${state.snapshot.name}。人工录入记录，尚未核验。`:'尚未添加经营记录。先添加来源，再比较经营假设。';
 return `${screenHero(id)}${journey(id)}<div class="record-surface-heading"><h2>${e(s.label)}</h2><span class="status-pill">${unknown?'待恢复':state.snapshot?'人工录入 · 尚未核验':'从第一条记录开始'}</span></div><p class="record-explanation">${e(message)}</p>${s.keys.length?metricCards(s.keys,state.snapshot,unknown):`<div class="research-boundary"><span aria-hidden="true">◎</span><div><b>先建立人群证据</b><p>${e(message)}</p></div>${btn('整理研究任务','data-work-draft="task" data-origin="persona"')}</div>`}${actionCards(id)}${id==='overview'?`<div class="overview-bottom">${sourceCard(state.snapshot,unknown,place)}<section class="work-progress"><div class="section-heading"><h3>当前工作成果</h3><span class="muted">仅统计本工作区</span></div><div class="progress-counts">${[['行动草稿',state.drafts.length,'content'],['已存情景',state.scenarios.length,'experiment'],['经营报告',state.reports.length,'report']].map(([label,count,to])=>`<button data-view="${to}"><strong>${unknown?'—':count}</strong><span>${label}</span></button>`).join('')}</div><p>草稿尚未对外执行，情景不是预测。保存后的结果可继续查看与整理。</p>${btn('整理经营报告','data-view="report"')}</section></div>`:''}<div class="legacy-actions toolbar">${btn('创建行动草稿','data-new-draft="task"')}${id==='overview'?btn('比较经营情景','data-view="experiment"'):''}</div>`;
}
export function syncWorkbench(id){
 if(!SCREENS[id])return;document.body.dataset.activeScreen=id;
 const crumb=document.querySelector('#currentScreen');if(crumb)crumb.textContent=SCREENS[id].label;
 for(const route of ['world','experiment','report']){const section=document.querySelector('#view-'+route);if(section&&!section.querySelector('[data-screen-hero]'))section.insertAdjacentHTML('afterbegin',screenHero(route)+journey(route));}
 document.querySelectorAll('#appNav [data-view]').forEach(b=>{const s=SCREENS[b.dataset.view];b.title=s.label+' · '+s.en;const label=b.querySelector('span:last-child');if(label)label.textContent=s.label;});
}
export function draftSeed(origin,kind,purpose='',context=''){
 const s=SCREENS[origin]||SCREENS.overview;
 const templates={persona:'人群假设：\n已有证据：\n待收集的行为 / 订单证据：\n验证方法：',content:'目标人群：\n核心信息：\n经营依据：\n内容形式：\n需要人工确认的事项：',live:'直播目标：\n关联商品：\n讲解要点：\n开播前待确认事项：\n复盘依据：',growth:'投放目标：\n关联内容与商品：\n经营依据：\n情景假设：\n风险与待确认事项：',product:'关联商品 / SKU：\n需要核对的来源：\n消费者问题：\n验证方法：'};
 return {title:s.label+(purpose?' · '+purpose:'')+' · '+({brief:'内容提纲',plan:'行动计划',task:'研究任务'}[kind]||'行动草稿'),body:(context?'前序背景：'+context+'\n\n':'')+(templates[origin]||'目标：\n依据：\n下一步：')};
}
