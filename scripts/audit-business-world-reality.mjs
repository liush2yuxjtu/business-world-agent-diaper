// Source-string scoring and old Markdown claims cannot establish runtime reality.
// This gate now requires complete real HTTP/browser/tool/database evidence tied
// to the current source, and explicitly reports the verified deployment profile.
import './check-shared-audit.mjs';
